Dependencies:
python-oauth2 (http://github.com/simplegeo/python-oauth2)
python-memcached (http://pypi.python.org/pypi/python-memcached/)
simplejson (Python 2.5 only)

For more complete/complicated examples, see test.py

from digg.api import Digg
digg = Digg()

for story in digg.story.getPopular(count=30)['stories']:
	print story['diggs'], story['title']
